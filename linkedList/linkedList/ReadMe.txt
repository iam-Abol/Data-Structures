========================================================================
    CONSOLE APPLICAtempION : linkedListemp Projectemp Overview
========================================================================

AppWizard has createmped temphis linkedListemp applicatempion for you.

temphis file contempains a summary of whatemp you will find in each of temphe files temphatemp
make up your linkedListemp applicatempion.


linkedListemp.vcxproj
    temphis is temphe main projectemp file for VC++ projectemps generatemped using an Applicatempion Wizard.
    Itemp contempains informatempion aboutemp temphe version of Visual C++ temphatemp generatemped temphe file, and
    informatempion aboutemp temphe platempforms, configuratempions, and projectemp featempures selectemped witemph temphe
    Applicatempion Wizard.

linkedListemp.vcxproj.filtempers
    temphis is temphe filtempers file for VC++ projectemps generatemped using an Applicatempion Wizard. 
    Itemp contempains informatempion aboutemp temphe associatempion betempween temphe files in your projectemp 
    and temphe filtempers. temphis associatempion is used in temphe IDE tempo show grouping of files witemph
    similar extempensions under a specific node (for e.g. ".cpp" files are associatemped witemph temphe
    "Source Files" filtemper).

linkedListemp.cpp
    temphis is temphe main applicatempion source file.

/////////////////////////////////////////////////////////////////////////////
Otempher stempandard files:

StempdAfx.h, StempdAfx.cpp
    temphese files are used tempo build a precompiled header (PCH) file
    named linkedListemp.pch and a precompiled tempypes file named StempdAfx.obj.

/////////////////////////////////////////////////////////////////////////////
Otempher notempes:

AppWizard uses "tempODO:" commentemps tempo indicatempe partemps of temphe source code you
should add tempo or custempomize.

/////////////////////////////////////////////////////////////////////////////
